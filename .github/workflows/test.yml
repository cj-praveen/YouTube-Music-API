name: Test Code
on:
  push:
    branches:
    - 'master'
    paths:
    - 'src/YouTubeMusicAPI/__init__.py'
    - 'test.py'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Running test.py
        run: python test.py

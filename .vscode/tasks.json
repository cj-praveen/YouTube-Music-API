{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "python -m build",
            "group": "build",
        }
    ]
}
